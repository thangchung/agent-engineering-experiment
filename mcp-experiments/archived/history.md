# 09 May 2026 - prompts for researching, planning 

```
read archived\research_v1.md, https://github.com/hyperlight-dev/hyperlight-sandbox/tree/main/src/sdk/dotnet, https://github.com/hyperlight-dev/hyperlight-sandbox/tree/main/examples, https://github.com/hyperlight-dev/hyperlight-sandbox/blob/4f89124275ac99652e41a0bd346ab94bbb85d39c/src/sdk/dotnet/core/Extensions.AI/CodeExecutionTool.cs#L138 in depth, understand how it works deeply, what it does and all its specificities. Current code base, there are 2 sandbox provioders: local and opensandbox, I would like to add 1 more provider for hyperlight sanbox (all privide links) with CodeAct pattern in C#/.NET, check the code on how to adapt with minimum changes (current code based run on cross-patform: linux, windows and macos). when that’s done, write a detailed report of your learnings and findings in research.md. Don’t implement anything (but pseudocode and workflows in mermaid format are required), just research and update markdownfile using caveman mode
```

```
reload .github\copilot-instructions.md, read research.md in deep. write/update a detailed plan.md document outlining how to implement this. include code snippets and workflows in mermaid format are required. use caveman mode
```

```
check research.md and plan.md once more time, I would like to support Windows by default, enable Hyperlight sandbox is default. workflows in mermaid format are required for easy understanding the approach. update research.md and plan.md with these requirements. on plan.md put the milestone with checklist so that we can track the work items on the top page. use caveman mode
```

```
read research.md in deep, check if we can add https://github.com/pythonnet/pythonnet to check /validate the code generated by LLM to make sure it is a correct python script, like `using (Py.GIL())
{
    try 
    {
        // Path to your script and the mode 'exec' for a full script
        PythonEngine.Compile(File.ReadAllText("script.py"), "script.py", RunFlagType.File);
        Console.WriteLine("Syntax is valid.");
    }
    catch (PythonException ex)
    {
        Console.WriteLine("Syntax Error: " + ex.Message);
    }
}
` or not?. no implement code, update research.md and plan.md if this solution is possible, if not then tell me and dont do anything, use caveman mode
```

```
but is it work like if LLM generated the wrong pythong (error when validate via preflight), then it will tell LLM to re-genareting code again. is it work that way? if not I wouild like to setup the agentic LLM gen code like this. if it already work like this, dont do anything, if not please update research.md and plan.md. use caveman mode
```

```
read https://github.com/github/copilot-sdk/blob/main/docs/auth/byok.md in deep, add another support for byok (beside of only using github copilot key), and let it configure in appsettings.json. update research.md and plan.md to include this feature. no implmemt, just pseudocode and workflow needed. use caveman mode
```

```
implement it all (ref plan.md - check milestones section). when you’re done with a task or phase, mark it as completed (in Milestones & Tracking Checklist section) in the plan document. do not stop until all tasks and phases are completed. do not add unnecessary comments or jsdocs, do not use any or unknown types. continuously run typecheck to make sure you’re not introducing new issues. make sure the current behavior not changes and still work in practical way. use caveman mode
```

```
/aspire query the logs of mcp-server and fix error in the hyperlight-sandbox. no restart aspire need, just query and fix. use caveman mode
```