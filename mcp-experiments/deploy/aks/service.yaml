apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: mcp-experiments
spec:
  selector:
    app: mcp-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
