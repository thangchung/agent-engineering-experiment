# GitHub Personal Access Token — get via: gh auth token
GITHUB_TOKEN=your_github_pat_here

# MCP server HTTP URLs (match --port when starting servers with --transport http)
ORDERS_MCP_URL=http://localhost:8001/mcp
PRODUCT_ITEMS_MCP_URL=http://localhost:8002/mcp

# MAF Agent Host URL (AG-UI endpoint, started via: uv run agent-host/main.py)
AGENT_HOST_URL=http://localhost:8000
